## discover Abstractness via specification examples

```python
# UNCERTAIN
Function[Self, int] ... Function[int]

Function[int, str] ... Function[int]
Function[int, int] ... Function[int]


Function[Name, Money] > Function[i

Function[MutableSequence] ... Function[list]

Function[Money] -> int  ... Function[int] -> Money

Function[Money] ... Money

hackathon
12'th June deadline



## product today

- sense dependencies
  - via imports
  - via mutation testing


MVP goal:
- pivot to a pyreverse wrapper product
- exposed via MCP or agent-agent P?
- expose raw scan output for File Dependencies AND UML Class Diagram
- provide Report(s)?
-




# CERTAIN
Function[Name] > Function[str]
Function[Money] > Function[int] # semantic abstraction AND?? variance
    Function[Money]
    Money = NewType(int) # value object
    Money[value: int, uuid: str] # entity
    Money = Callable[[int], int]
Money > int
FirstName > str
int > 10
```
