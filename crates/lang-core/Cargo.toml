[package]
name = "lang-core"
version = "0.1.0"
edition = "2024"
description = "Language-agnostic dependency model and analyzer trait."
license = "AGPL-3.0-or-later"
authors = ["Better Bearings"]

[dependencies]
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
