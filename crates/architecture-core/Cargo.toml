[package]
name = "architecture-core"
version = "0.1.0"
edition = "2024"
description = "Pure domain model and metrics for architecture analysis."
license = "AGPL-3.0-or-later"
authors = ["Better Bearings"]

[dependencies]
thiserror = "2.0"

[lints]
workspace = true
